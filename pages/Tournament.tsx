import React from 'react';
import WipPlaceholder from '../components/WipPlaceholder';

const Tournament: React.FC = () => {
    return (
        <WipPlaceholder backPath="/minecraft" />
    );
};

export default Tournament;
